# consumer-goods-labels

An informational website about the different consumer goods labels available in Sweden

## Local development

The site is plain HTML/CSS/JS with no build step.

**Using Node.js (`npx`):**

```bash
npx serve .
```

Then open the URL printed in the terminal.
